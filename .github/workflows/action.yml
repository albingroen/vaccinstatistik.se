name: "Update data"
description: 'Fetch data from Folkhälsomyndigheten and update the local json file'

on:
  schedule:
    - cron: '0 1 * * *'

jobs:
  update-data:
    name: Update data
    steps:
      - name: Update json file
        run: node ./update-data.js
